#include "StringCalculator.h"
#include<string.h>
#include <ctype.h>

int isEmpty(const char* input){
  return (isdigit(*input));
}

int add(const char*input{
  if(!(isEmpty(input))){
      char result[0]='\0';
   }
  return result;
}
  

